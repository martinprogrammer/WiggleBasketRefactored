@model WiggleBasketRefactored.Models.Products.ProductSpecific

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>ProductSpecific</legend>

    <div class="display-label">ProductID</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.ProductID)
    </div>

    <div class="display-label">RuleName</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.RuleName)
    </div>

    <div class="display-label">RuleValue</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.RuleValue)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
